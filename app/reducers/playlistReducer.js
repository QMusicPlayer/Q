'use strict';

var _ = require('lodash');
//view strings
const initial = {
  
}
function playlistReducer (state, action){
  state = state || initial;
  switch (action.type){
    default:
      return state;
  }
};

module.exports = playlistReducer;
